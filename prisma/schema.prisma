generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_URI")
}

model Artist {
  id String @id @default(uuid())

  name String
  genre String

  @@map("artists")
}